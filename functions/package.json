{
  "name": "amt-imaging-functions",
  "description": "AMT Imaging Service App Cloud Functions (Grok backup search). xAI key is never committed — set XAI_API_KEY in Cloud Secret Manager.",
  "private": true,
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "scripts": {
    "check": "node --check index.js && node --check query-guard.js && node query-guard.test.js"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^6.3.2"
  }
}
